import 'package:flutter/material.dart';

class AppColors {
  // Primary: Hijau padi yang segar dan natural
  static const primary = Color(0xFF2E7D32);        // Hijau tua padi
  static const primaryLight = Color(0xFF66BB6A);   // Hijau muda daun
  static const primaryDark = Color(0xFF1B5E20);    // Hijau sangat tua
  
  // Secondary: Kuning padi matang
  static const secondary = Color(0xFFF9A825);      // Kuning padi
  static const secondaryLight = Color(0xFFFFD54F);
  
  // Accent: Coklat tanah
  static const soil = Color(0xFF795548);
  static const soilLight = Color(0xFFBCAAA4);
  
  // Status
  static const danger = Color(0xFFE53935);         // Hama parah
  static const warning = Color(0xFFFF8F00);        // Hama sedang
  static const success = Color(0xFF43A047);        // Sehat
  static const info = Color(0xFF0288D1);           // Info
  
  // Background
  static const background = Color(0xFFF1F8E9);    // Hijau sangat muda
  static const surface = Colors.white;
  static const cardBg = Color(0xFFFFFDE7);         // Cream lembut
}
